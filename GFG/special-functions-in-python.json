{
    "status": 200,
    "message": "Question Data fetched successfully",
    "data": {
        "question": {
            "questionFrontendId": 879280,
            "title": "Special Functions In Python",
            "titleSlug": "special-functions-in-python",
            "content": "<p><span >Given two lists of integers, perform the following operations:</span></p>\n<ol>\n<li>\n<p><span ><strong><code>zip()</code></strong>: Combine both lists into one iterable of tuples.</span></p>\n</li>\n<li>\n<p><span ><strong><code>filter()</code></strong>: Filter out all elements in <code>list1</code> that are greater than <code>2</code>.</span></p>\n</li>\n<li>\n<p><span ><strong><code>map()</code></strong>: Multiply each element of <code>list1</code> by <code>2</code> using a <code>lambda</code> function.</span></p>\n</li>\n</ol>\n<p><span >You need to complete the given program.</span></p>\n<p><strong><span >Examples:</span></strong></p>\n<pre><strong><span >Input: </span></strong><span >list1 = [</span><span style=\"font-size: 18.6667px;\">1, 2, 3, 4, 5], list2 = [</span><span ><span >6, 7, 8, 9, 10]<br /></span><strong >Output:<br /></strong><span style=\"font-size: 18.6667px;\">[(1, 6), (2, 7), (3, 8), (4, 9), (5, 10)]<br />[1, 2]<br /></span></span><span style=\"font-size: 18.6667px;\">[2, 4, 6, 8, 10]</span></pre>",
            "isPaidOnly": false,
            "difficulty": "Easy",
            "likes": "NA",
            "dislikes": "NA",
            "rephrased": {
                "body": "Given two lists of integers, perform the following operations:\n\n1. **`zip()`**: Combine both lists into one iterable of tuples.\n2. **`filter()`**: Filter out all elements in `list1` that are greater than `2`.\n3. **`map()`**: Multiply each element of `list1` by `2` using a `lambda` function.\n\nYou need to complete the program to achieve the specified transformations on the input lists.",
                "constraints": [],
                "testcases": [
                    {
                        "input": "list1 = [1, 2, 3, 4, 5], list2 = [6, 7, 8, 9, 10]",
                        "output": "[(1, 6), (2, 7), (3, 8), (4, 9), (5, 10)]\n[1, 2]\n[2, 4, 6, 8, 10]",
                        "explanation": "First, the lists are zipped together. Then, `list1` is filtered to keep elements less than or equal to 2. Finally, the original `list1`'s elements are doubled using a lambda function within a map operation."
                    }
                ]
            }
        },
        "similar": [],
        "hints": [],
        "relatedTopics": [],
        "companies": [],
        "solutions": {
            "py": {
                "lang": "Python3",
                "full_func": "#Back-end complete function Template for Python 3\nlist1 = list(map(int, input().split()))\nlist2 = list(map(int, input().split()))\n\n# 1. Use zip() to combine multiple lists into a single iterable\nzipped = zip(list1, list2)\nprint(list(zipped))  # Converts to list for display\n\n# 2. Use filter() with lambda function to filter out numbers greater than 2 in list1\nfiltered = filter(lambda x: x <= 2, list1)\nprint(list(filtered))\n\n# 3. Using map() with lambda function to multiply each number of list1 by 2\nmapped = map(lambda x: x * 2, list1)\nprint(list(mapped))\n",
                "lang_text": "py"
            }
        },
        "platform": "GFG"
    }
}